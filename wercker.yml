deploy:
  steps:
    - bash-template:
        cwd: $WERCKER_ROOT

    - kubectl:
        server: $KUBERNETES_MASTER
        token: $KUBERNETES_TOKEN
        insecure-skip-tls-verify: true
        command: apply -f $WERCKER_ROOT/inventory-cassandra-OKE.yaml
